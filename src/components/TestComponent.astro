---
import astroLogo from '../assets/astro.svg';
import background from '../assets/background.svg';
import { STRAPI_URL } from 'astro:env/server';

// const response = await fetch(`${STRAPI_API_URL}/api/articles`);
const sPath = `${STRAPI_URL}/api/articles`;


// CONST base_api_url = import.meta.VITE_STRAPI_BASE_URL ?? "http://localhost:1337";
    // const BASE_API_URL = import.meta.VITE_STRAPI_BASE_URL ?? "http://localhost:1337";
    // const BASE_API_URL = import.meta.env.STRAPI_BASE_URL;
    // const path = "/api/global";
    // const url = new URL(path, BASE_API_URL);
    // const response = await fetch(url.href);

async function getStrapiGlobalData(){
  const response = await fetch('https://dummyjson.com/todos');
  const data = await response.json();
  console.dir(data,{depth: null});
  return data;

}
const globalData = await getStrapiGlobalData();
---
  
  <!-- Navbar -->
  <nav class="max-w-7xl mx-auto px-6 py-6 flex justify-between items-center">
    <h1 class="text-2xl font-bold tracking-tight">Nova<span class="text-indigo-500">.</span></h1>
    <div class="hidden md:flex gap-8 text-sm text-slate-300">
      <a href="#" class="hover:text-white">Features</a>
      <a href="#" class="hover:text-white">Pricing</a>
      <a href="#" class="hover:text-white">Contact</a>
    </div>
    <button class="bg-indigo-600 hover:bg-indigo-500 transition px-5 py-2 rounded-full text-sm font-medium">
      Get Started
    </button>
  </nav>
  {sPath.length>10 && <div>{sPath}</div>}
<pre>{JSON.stringify(globalData)}</pre>
  <!-- Hero -->
  <section class="relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-indigo-600/20 via-purple-600/10 to-transparent blur-3xl"></div>

    <div class="relative max-w-7xl mx-auto px-6 py-28 text-center">
      <h2 class="text-5xl md:text-6xl font-extrabold tracking-tight leading-tight">
        Build Stunning <br />
        <span class="bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">
          Digital Experiences
        </span>
      </h2>

      <p class="mt-6 max-w-2xl mx-auto text-lg text-slate-300">
        Launch beautiful, fast, and scalable web products with a modern Tailwind-powered design system.
      </p>

      <div class="mt-10 flex justify-center gap-4">
        <button class="bg-indigo-600 hover:bg-indigo-500 transition px-8 py-3 rounded-full font-medium">
          Start Free
        </button>
        <button class="border border-slate-700 hover:border-slate-500 transition px-8 py-3 rounded-full font-medium">
          Live Demo
        </button>
      </div>
    </div>
  </section>

  <!-- Logos -->
  <section class="max-w-7xl mx-auto px-6 py-16">
    <p class="text-center text-sm text-slate-400 mb-8">
      Trusted by teams worldwide
    </p>
    <div class="flex flex-wrap justify-center gap-10 text-slate-500 font-semibold">
      <span>Google</span>
      <span>Stripe</span>
      <span>Shopify</span>
      <span>Meta</span>
      <span>Netflix</span>
    </div>
  </section>

  <!-- Features -->
  <section class="max-w-7xl mx-auto px-6 py-24 grid md:grid-cols-3 gap-8">
    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-8 hover:border-indigo-500 transition">
      <h3 class="text-xl font-semibold mb-3">Lightning Fast</h3>
      <p class="text-slate-400">
        Optimized layouts and minimal CSS for blazing-fast performance.
      </p>
    </div>

    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-8 hover:border-indigo-500 transition">
      <h3 class="text-xl font-semibold mb-3">Modern Design</h3>
      <p class="text-slate-400">
        Pixel-perfect UI with smooth animations and clean typography.
      </p>
    </div>

    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-8 hover:border-indigo-500 transition">
      <h3 class="text-xl font-semibold mb-3">Scalable</h3>
      <p class="text-slate-400">
        Built to grow with your product and business needs.
      </p>
    </div>
  </section>

  <!-- Testimonial -->
  <section class="max-w-4xl mx-auto px-6 py-24 text-center">
    <blockquote class="text-2xl font-medium leading-relaxed">
      “This landing page increased our conversion rate by over 40%.  
      Clean, fast, and absolutely beautiful.”
    </blockquote>
    <p class="mt-6 text-slate-400">— Product Manager, SaaS Company</p>
  </section>

  <!-- Pricing -->
  <section class="max-w-7xl mx-auto px-6 py-24 grid md:grid-cols-3 gap-8">
    <div class="border border-slate-800 rounded-2xl p-8 text-center">
      <h3 class="text-lg font-semibold mb-4">Starter</h3>
      <p class="text-4xl font-bold mb-6">$0</p>
      <button class="w-full border border-slate-700 py-3 rounded-full">
        Get Started
      </button>
    </div>

    <div class="bg-indigo-600 rounded-2xl p-8 text-center shadow-xl scale-105">
      <h3 class="text-lg font-semibold mb-4">Pro</h3>
      <p class="text-4xl font-bold mb-6">$29</p>
      <button class="w-full bg-white text-indigo-600 py-3 rounded-full font-semibold">
        Buy Now
      </button>
    </div>

    <div class="border border-slate-800 rounded-2xl p-8 text-center">
      <h3 class="text-lg font-semibold mb-4">Enterprise</h3>
      <p class="text-4xl font-bold mb-6">$99</p>
      <button class="w-full border border-slate-700 py-3 rounded-full">
        Contact Sales
      </button>
    </div>
  </section>

  <!-- CTA -->
  <section class="max-w-7xl mx-auto px-6 py-24 text-center">
    <h2 class="text-4xl font-bold mb-6">
      Ready to launch something amazing?
    </h2>
    <button class="bg-indigo-600 hover:bg-indigo-500 transition px-10 py-4 rounded-full font-semibold">
      Get Started Today
    </button>
  </section>

  <!-- Footer -->
  <footer class="border-t border-slate-800 py-10 text-center text-sm text-slate-500">
    © 2025 Nova. All rights reserved.
  </footer>